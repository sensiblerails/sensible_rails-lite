<div class="max-w-4xl mx-auto">
  <div class="bg-white shadow rounded-lg p-6 mb-8">
    <h1 class="font-bold text-3xl mb-4">Welcome to Sensible Rails Lite</h1>
    <p class="text-gray-600 mb-4">A lean, modern template for experimenting with core Rails concepts and latest built-in Rails technologies.</p>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-6">
      <div class="bg-blue-50 p-4 rounded shadow-sm">
        <h2 class="font-semibold text-xl mb-2">Features</h2>
        <ul class="list-disc pl-5 space-y-1">
          <li>Vanilla Rails authentication</li>
          <li>User impersonation for admins</li>
          <li>Simple authorization</li>
          <li>SolidCache, SolidQueue, SolidCable</li>
          <li>Hotwire (Turbo and Stimulus)</li>
          <li>Real-time notifications</li>
        </ul>
      </div>
      
      <div class="bg-green-50 p-4 rounded shadow-sm">
        <h2 class="font-semibold text-xl mb-2">Get Started</h2>
        <% if logged_in? %>
          <p>You're logged in as <strong><%= current_user.name %></strong>.</p>
          <p class="mt-2">Check out <%= link_to "all posts", posts_path, class: "text-blue-600 hover:underline" %> or <%= link_to "create a new post", new_post_path, class: "text-blue-600 hover:underline" %>.</p>
        <% else %>
          <p>Please <%= link_to "sign up", signup_path, class: "text-blue-600 hover:underline" %> or <%= link_to "log in", login_path, class: "text-blue-600 hover:underline" %> to get started.</p>
        <% end %>
      </div>
    </div>
  </div>
  
  <% if @posts.any? %>
    <div class="bg-white shadow rounded-lg p-6">
      <h2 class="font-bold text-2xl mb-4">Recent Posts</h2>
      
      <div class="space-y-4">
        <%= render partial: "posts/post", collection: @posts %>
      </div>
      
      <div class="mt-4 text-center">
        <%= link_to "View All Posts", posts_path, class: "inline-block bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded" %>
      </div>
    </div>
  <% end %>
</div>
